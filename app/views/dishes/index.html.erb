<section class="content">
  <div class="row">
  
    <div class="col-xs-12">
      <div class="text-right margin-bottom">
        <a type="button" class="btn btn-warning" data-toggle="modal" data-target="#create_dish">
          <i class="fa fa-plus"></i>&nbsp; Create Dish
        </a>
        <span data-action_button class="hidden">
          <a type="button" class="btn btn-danger" data-toggle="modal" data-target="#delete_dishes">
            <i class="fa fa-trash"></i>&nbsp; Delete Dish
          </a>
        </span>
      </div>
      
      <div class="box">
        <div class="box-header">
          <h3 class="box-title"><b>Dish Management</b></h3>
        </div>

        <div class="box-body table-responsive">
          <!-- CHANGE HERE TO RUN index.js.erb -->
          <div id="dishes">
            <%= render 'table' %>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>


<!-- Modal create -->
<div class="modal fade" id="create_dish" tabindex="-1" role="dialog" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <%= render 'form' %>
      </div>
    </div>
  </div>
</div>

<!-- Modal delete -->
<%= form_with url: destroy_multiple_dishes_path, method: :delete, id: 'delete_dishes_form', local: true do |f| %>
  <%= f.hidden_field :dish_ids %>
  <div class="modal fade" tabindex="-1" role="dialog" id="delete_dishes">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-body">
          <p>Are you sure to delete these dishes ?</p>
        </div>
        <div class="modal-footer">
          <button type="submit" class="btn btn-primary">Yes</button>
          <button type="button" class="btn btn-secondary" data-dismiss="modal">No</button>
        </div>
      </div>
    </div>
  </div>
<% end %>